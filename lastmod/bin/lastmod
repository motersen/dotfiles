#!/usr/bin/env perl

use strict;
use warnings;

my $lines = shift @ARGV || 1;
my $dir = shift @ARGV || "";

open my $files, "ls -1t $dir |";
while ($lines-- and my $line = <$files>) {
    print $line;
}
close $files;
